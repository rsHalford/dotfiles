[Unit]
Description=Llama Swap Proxy Server
After=network.target

[Service]
ExecStart=llama-swap --config %h/.config/llama-swap/config.yaml --listen 0.0.0.0:8033
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
